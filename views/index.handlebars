<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Earthquakes</title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/foundation/6.3.1/css/foundation.min.css">
        <link rel="stylesheet" href="/public/styles.css">
        <link href='https://fonts.googleapis.com/css?family=Open+Sans:400,700' rel='stylesheet' type='text/css'>
    </head>
    <body>
        <header id="top-info">
            <h1>Earthquakes</h1>
            <p>Info on all the recent earthquake activity in the United States 🇺🇸</p>
            <aside>*Limited to the 10 most recent results*</aside>
        </header>

        <main>

            {{#each earthquakes}}
                <section class="single-earthquake row">
                    <div class="small-12 columns">
                        <h2>{{properties.title}}</h2>
                        <div class="row">
                            <div class="info small-12 large-5 large-push-1 columns">
                                <p>Location: <a href="{{properties.url}}">{{properties.place}}</a></p>
                                <p>Coodinates: {{ geometry.coordinates}}</p>
                            </div>
                            <div class="info small-12 large-5 large-push-1 columns">
                                <p>Magnitude: {{properties.mag}}</p>
                                <p>Type: {{ properties.type}}</p>
                            </div>
                        </div>
                    </div>
                    <div class="small-12 columns">
                        <iframe src="{{properties.url}}#map" width="100%" height="500px"></iframe>
                    </div>
                </section>
            {{/each}}

        </main>

        <footer class="center">Created by Matt Fewer. Hope you liked it! 😊</footer>



        <script src="/public/script.js" charset="utf-8"></script>
    </body>
</html>
